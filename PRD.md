# IPIPX 985Proxy-compatible OpenAPI 家宽代理平台 PRD

## 1. 产品定位

IPIPX 是一个面向静态住宅代理交易、交付和分销的 **985Proxy-compatible OpenAPI 家宽代理平台**。

平台不是单一上游转售站，也不是在主站数据库里维护无限层级代理树的传统分销系统。IPIPX 的核心形态是：每个站点都暴露一套普通的 985Proxy 风格下单 API，下游系统只把它当成普通 Provider 使用。

- 主站负责真实 Provider 聚合、静态住宅代理采购、履约、交付、订单、钱包、审计和 OpenAPI。
- Reseller 分站复用主站产品能力和前端体验；一个分站站点可以承载多个代理商租户，代理商共享分站前端、域名、部署和数据库，但客户、余额、价格、APIKey、订单和审计必须按代理商隔离。
- 每一层平台都可以拿上游 APIKey 向直接上游采购，同时给自己的客户或下游系统签发本层 APIKey。
- 每一层只处理自己的直接上下游，不关心下下级是谁，也不跨层计算利润。

所谓“级联”只描述部署和采购链路，不是要发明一套多级代理协议。公开接口应优先兼容 985Proxy 的 API 风格：`apikey` header、`code/msg/data` 响应、`/res_static/*` 静态住宅接口。长期产品细节统一维护在本 PRD，技术落地统一维护在 `docs/ARCH.md` 和 Trellis task 中。

## 2. 当前产品主线

### 2.1 P0 主线：静态住宅代理

当前优先级是打穿静态住宅代理真实闭环：

1. 用户登录，不手动输入数据库 ID。
2. Admin 配置上游 Provider 或上游 OpenAPI。
3. 同步上游商品、地区、库存和参考价格。
4. Admin 将上游资源映射为本层平台资源树。
5. Admin 设置本层价格模板、资源覆盖价和用户覆盖价。
6. 用户或 OpenAPI 客户获取本层报价。
7. 用户用本层虚拟余额或支付能力下单。
8. 本层向直接上游转发采购。
9. 上游真实履约并返回代理。
10. 本层保存订单镜像和代理实例镜像。
11. 用户查看、复制、导出静态代理。
12. Admin 可追踪订单、上游请求、失败原因、重试、退款、补单和审计。

P0 静态住宅基础功能必须包含：

- IP 开出来后像 IPIPD 一样提供直接复制入口。
- 切 IP 成功后弹出新代理信息，并能直接复制。
- 代客下单模式：Admin 或代理商从客户详情进入购买流程，不手输数据库 ID。
- IP 续费搜索：按 IP、订单号、客户、国家、状态、到期时间定位可续费资源。
- 首批只围绕 PR、IPIPD 推荐 IP、985Proxy 三类 Provider 打通真实资源、库存、报价、下单和交付。

### 2.2 降级方向：动态住宅代理

动态住宅代理不是当前主力方向。第一阶段允许保留入口和产品方向，但必须明确显示真实状态：

- 未接入真实能力时，不展示假账号、假流量、假 endpoint、假 API 链接或假代理。
- 用户侧应显示“动态住宅暂缓接入”或结构化不可用原因。
- OpenAPI 应返回明确的 `UNSUPPORTED_CAPABILITY`。
- Admin 应能看到动态能力未启用、缺 Provider、缺价格或缺履约配置的原因。

动态住宅后续作为独立切片生产化，不能阻塞静态住宅主线。

## 3. 平台角色

### 3.1 主站 IPIPX

主站承担真实平台能力：

- 管理 P0 真实 Provider：PR、IPIPD、985Proxy。
- 管理 Provider 凭据、能力、健康、库存、请求日志。
- 对外提供 985Proxy-compatible OpenAPI。
- 给直接客户签发 APIKey。
- 为直接客户提供报价、下单、订单查询、代理实例查询。
- 管理主站客户的钱包、支付、账本和订单。
- 处理 Provider 失败、退款、补单、生命周期和审计。

主站只知道自己的直接客户。例如老王用主站 APIKey 下单，主站只记录老王的订单、扣款和交付，不关心老王是否继续转售。

### 3.2 Reseller 分站

Reseller 是第一类共享型下游分站站点。它不是“每个代理商一套独立分站”，而是一套分站站点承载多个代理商租户：

- 使用与主站共享的前端和后端代码模板。
- 作为一个共享分站站点独立部署为自己的域名和品牌。
- 多个代理商共享 Reseller 的前端、域名、部署、数据库和基础上游能力。
- 每个代理商租户拥有自己的客户、钱包、价格、订单、代理镜像、APIKey 和审计视图。
- 分站站点可以配置公共上游 APIKey；如业务需要，也可以允许代理商租户配置自己的上游 APIKey。
- 代理商给自己的客户或下游系统签发本租户 APIKey。
- 代理商可以自由给自己名下客户充值、扣款和调账，但不能操作其他代理商客户。

Reseller 不读取主站数据库，不知道主站底层 Provider 凭据，只通过上游 985Proxy-compatible OpenAPI 拿货。

### 3.3 共享分站租户隔离

共享分站必须在所有业务对象上带租户边界：

- `resellerId` 或 `tenantId` 标识代理商租户。
- 客户归属到某个代理商租户。
- 客户 APIKey 归属到某个代理商租户。
- 客户钱包和账本只在该代理商租户下可见。
- 价格模板、用户价格覆盖、资源可见性可按代理商租户隔离。
- 订单、上游订单镜像、代理实例镜像按代理商租户隔离。
- Admin 或代理商工作台查询必须默认带租户过滤。
- 审计日志必须记录操作者、代理商租户、目标客户和业务对象。

共享分站可以有平台级超级 Admin，用于管理所有代理商、公共上游、公共资源和系统健康；代理商只能管理自己的客户、价格、余额、APIKey 和订单。

### 3.4 分站运营能力

分站站点应当具备接近主站的运营能力，只是数据范围限定在分站及其代理商租户内。

分站平台级 Admin 必须能管理：

- 分站站点配置、品牌、域名、公告和维护状态。
- 公共上游 API、凭据、连通测试、同步任务和请求日志。
- 公共资源树、国家、地域、可用区、IP 类型、库存、可售状态和不可售原因。
- 全局价格覆盖。
- 全局价格模板。
- 分等级价格表。
- 按国家 / 地域 / 可用区 / IP 类型设置价格。
- 代理商租户列表、状态、权限、客户数量、余额汇总和订单汇总。
- 用户管理、用户状态、用户风险状态、KYC 状态和 impersonation。
- 用户级价格覆盖，覆盖维度包括国家、地域、可用区、IP 类型、时长和数量。
- 订单管理、上游订单镜像、代理实例镜像、重试、退款、补单和审计。
- 充值、扣款、调账、账本、退款和对账。
- 统计报表，包括充值、消费、订单、交付成功率、库存、Provider / 上游失败率和代理商维度汇总。
- APIKey、OpenAPI 调用日志、限流事件和 Webhook。

代理商租户 Admin 必须能管理：

- 自己的客户。
- 自己客户的充值、扣款、调账和账本。
- 自己客户的 APIKey。
- 自己租户下的价格模板、用户价格覆盖和资源可见性。
- 自己租户下订单、静态代理实例、续费、改密、切 IP 和导出。
- 自己租户下统计报表和审计记录。

分站不能只做轻量代理后台；它应当是缩小数据范围后的主站运营台。

### 3.5 每层平台的双重角色

每层平台都同时是：

- **Upstream Consumer**：用上游 APIKey 拿货。
- **Downstream Provider**：用本层 OpenAPI 对外供货。

因此每层都必须具备：

- 配置上游 API。
- 同步上游商品、库存和价格。
- 映射成本层资源树。
- 设置本层或本租户价格覆盖和价格模板。
- 创建本层或本租户客户。
- 给本层或本租户客户充值虚拟余额。
- 给本层或本租户客户签发 APIKey。
- 提供本层 OpenAPI。
- 接收本层 UI / API 下单。
- 向直接上游转发采购。
- 保存本层订单镜像。
- 保存本层静态代理实例镜像。

## 4. 用户与入口

### 4.1 官网 / Landing Page

官网承担品牌展示、搜索转化、地区库存信号展示和购买入口。

必须包含：

- 首页：静态住宅代理定位、覆盖国家、稳定性、HTTP/SOCKS5、购买入口。
- 购买页：静态住宅代理地区、库存、价格、协议、时长、数量。
- Pricing：静态 IP 按地区、时长、数量计费；动态住宅明确标注暂缓或灰度状态。
- 工具：HTTP/SOCKS5 代理检测工具。
- 教程：浏览器、指纹浏览器、API、静态代理教程。
- 关于我们、推广返佣、合规条款和支持入口。

分站可以使用相同页面结构。共享分站的品牌、域名、Logo、站点名称和客服方式来自分站站点配置；用户登录后看到的价格、余额、订单、APIKey 和可见资源来自所属代理商租户配置。

### 4.2 用户控制台

用户控制台必须包含：

- 概览：余额、近期订单、即将过期、静态代理数量、动态能力状态、API Key 状态。
- 静态住宅代理购买。
- 我的静态代理。
- 充值 / 虚拟余额。
- 账单和钱包流水。
- API Key 管理。
- 代理验证工具。
- 优惠券。
- 实名 / 风控状态。
- 推广或邀请入口。
- 反馈和工单。

用户不应为了购买、导出、APIKey、动态能力或价格覆盖去手动查数据库 ID。

### 4.3 管理员后台

Admin 是运营工作台，不只是数据表格。

必须覆盖：

- 上游配置。
- Provider 管理。
- 上游商品 / 库存同步。
- 资源树编排。
- 价格模板和覆盖。
- 用户和 APIKey 管理。
- 钱包调账。
- 订单、上游订单镜像、代理实例镜像。
- 支付和充值。
- 生命周期操作。
- 动态住宅能力状态。
- KYC、客服、推广、报表。
- API 日志、限流事件、审计日志。

## 5. 级联订单链路

级联链路示例：

1. 主站提供真实静态住宅代理，Reseller 分站配置主站 APIKey 作为公共上游。
2. 老王和小李都是 Reseller 分站里的代理商租户，共享 Reseller 前端、域名、部署和数据库。
3. 老王在 Reseller 设置自己的客户、价格模板、用户覆盖价和 APIKey。
4. 小李在 Reseller 设置自己的客户、价格模板、用户覆盖价和 APIKey。
5. 老王客户在 Reseller 下单时，只进入老王租户的钱包、订单、价格和审计。
6. 小李客户在 Reseller 下单时，只进入小李租户的钱包、订单、价格和审计。
7. Reseller 用直接上游 APIKey 向主站采购。
8. 主站真实调用 Provider 采购并交付。
9. 主站结果返回 Reseller。
10. Reseller 按订单所属代理商租户保存订单镜像和代理实例镜像，并交付给对应客户。

每层只处理自己的直接关系：

- 分站客户不知道主站 APIKey。
- 老王不能看到小李客户、余额、价格、订单和 APIKey。
- 小李不能看到老王客户、余额、价格、订单和 APIKey。
- 主站只知道 Reseller 或其直接 APIKey 客户来下单。
- 不需要全局多级树。

## 6. 财务与钱包

### 6.1 本层虚拟余额

每层平台都有自己的客户钱包和账本。

钱包字段：

- 可用余额。
- 冻结余额。
- 币种。
- 最近更新时间。

账本类型：

- 充值。
- 扣款。
- 退款。
- 调整。
- 冻结。
- 解冻。
- 续费。
- 返佣。

所有余额变化必须写账本，不能只改余额字段。

### 6.2 分站充值边界

分站客户给分站充值，钱属于分站运营者，与主站无直接关系。

因此：

- 共享分站里的每个代理商可以自由给自己名下客户充值、扣款、调账。
- 代理商之间的钱包、账本和客户余额必须隔离。
- 主站不记录分站终端客户充值。
- 主站不计算分站利润。
- 主站不追踪分站终端客户。
- 主站只记录直接 API 客户的下单、扣款、退款和交付。

### 6.3 支付渠道

主站需要接入真实支付渠道：

- 余额支付。
- 易支付。
- 支付宝。

分站可以选择：

- 使用自己的线下收款后手动给客户充值。
- 接入自己的支付渠道。
- 暂不开放在线支付，只保留虚拟余额。

无论哪种方式，分站本层账本必须完整。

## 7. 价格体系

每层平台都有独立价格系统。本层价格只影响本层客户，不影响上游。

优先级：

1. 用户资源价格覆盖。
2. 用户绑定价格模板。
3. 用户等级价格表。
4. 资源级覆盖价。
5. 全局价格覆盖。
6. 全局价格模板。
7. 上游同步参考价或默认价。

价格能力必须支持：

- 静态住宅按国家、城市、线路、IP 类型、时长、数量配置价格。
- 全局价格模板。
- 分等级价格表，例如普通客户、代理客户、大客户和特殊客户等级。
- 用户绑定价格模板。
- 用户级价格覆盖。
- 全局价格覆盖。
- 资源可见性覆盖。
- 优惠券和折扣窗口。
- 报价沙盒：Admin 可模拟某个用户、资源、时长、数量下的最终价格。

上游价格变化不应自动改写本层售价，除非本层模板明确设置了跟随策略。

## 8. Provider 和上游能力

### 8.1 主站真实 Provider

主站统一抽象 Provider。静态住宅 P0 只启用三类 Provider：

- `IPIPD`：IPIPD OpenAPI v2。
- `NINE_EIGHT_FIVE`：985Proxy OpenAPI。
- `PR`：PR 静态住宅供应线。

ProviderAdapter 必须屏蔽上游差异，输出统一领域对象。

### 8.2 首批 Provider 国家范围

静态住宅代理第一阶段必须按真实 Provider 能力展示国家，不能把未接入国家做成静态 fallback。

P0 只允许展示以下三类 Provider 覆盖范围内的国家。其它国家必须隐藏或展示明确“暂未接入”状态，不能参与报价或下单。

PR 供应线首批覆盖：

- 新加坡。
- 泰国。
- 波兰。
- 巴西。
- 土耳其。
- 以色列。
- 荷兰。
- 印度。
- 加拿大。
- 奥地利。
- 罗马尼亚。
- 拉脱维亚。
- 乌克兰。

IPIPD 首批只对接它提供的推荐 IP 能力，不做非推荐 IP 冒充。首批覆盖：

- 英国。
- 法国。
- 德国。
- 意大利。
- 西班牙。
- 日本。
- 越南。
- 韩国。
- 阿联酋。
- 南非。

985Proxy 首批覆盖：

- 台湾。
- 菲律宾。
- 马来西亚。
- 澳大利亚。
- 香港。

Admin 资源编排必须展示每个国家来自哪个 Provider、是否推荐 IP、是否可售、库存更新时间、不可售原因和最近上游错误。

Provider 国家配置必须作为后端配置、数据库 seed 或 Admin 配置进入系统，不能硬编码在前端页面。

### 8.3 分站上游 OpenAPI

分站的上游可以不是底层 Provider，而是任意兼容 985Proxy 风格 OpenAPI 的上游平台。IPIPX 自己对外也要提供这套兼容 API，使 Reseller 可以直接把主站当成普通 985Proxy-like Provider 使用。

分站只需要知道：

- 上游 Base URL。
- 上游 APIKey。
- 上游鉴权方式，第一阶段优先支持 `apikey` header。
- 上游商品、报价、订单和代理实例接口。

分站不需要知道上游底层使用哪个真实 Provider。

共享分站支持两种上游模式：

- 平台公共上游：Reseller 配置一个公共上游 APIKey，多个代理商共享这一套上游采购能力。
- 租户自带上游：特定代理商租户配置自己的上游 APIKey，采购和镜像归属该租户。

第一阶段默认采用平台公共上游，租户自带上游作为可扩展能力写入架构。

### 8.4 必须支持的静态能力

静态住宅代理必须支持：

- 同步商品 / 地区 / 库存。
- 查询报价。
- 创建订单。
- 查询订单。
- 查询已购实例。
- 导出代理连接信息。
- 续费实例。
- 修改实例密码。
- 更换 IP。-- 根据具体的 Provider 能力而定
- 查询 Provider 或上游请求日志。
- 按国家、订单号、IP、状态、到期时间搜索续费对象。
- 代客下单模式：Admin 或代理商可从客户详情进入下单流程，按客户身份报价和扣款。

不支持的能力 家宽实例不渲染相关按钮/界面...

IPIPD 风格复制体验是 P0 验收要求：

- 新开 IP 交付完成后，订单结果和“我的静态代理”都必须显示复制按钮。
- 支持复制 `IP:PORT`、`IP:PORT:USERNAME:PASSWORD`、`USERNAME:PASSWORD@IP:PORT`、HTTP URL、SOCKS5 URL。
- 切 IP 成功后必须弹出新 IP、端口、账号、密码、协议和到期时间，并提供同样的复制格式。
- 批量续费、改密、切 IP 必须展示成功项、失败项和失败原因。
- 代客下单、续费搜索、切 IP 和复制都必须写审计。

## 9. 资源与库存

每层平台都维护自己的资源树。

资源来源：

- 主站从真实 Provider 同步资源。
- 分站从直接上游 OpenAPI 同步资源。

Admin 工作台必须支持：

- 右侧展示上游资源库。
- 左侧展示本层平台资源树。
- 将上游国家、城市、线路、可用区拖拽或选择映射到本层资源。
- 设置本层显示名称、排序、标签、可见性和可售状态。
- 设置资源到上游资源的路由权重。
- 为特殊客户提供更优线路或更细地区可见性。
- 禁用资源后立即影响本层购买页和报价 API。
- 管理首批 Provider 国家范围，包括 PR、IPIPD 推荐 IP 和 985Proxy 的国家、库存、可售状态和不可售原因。

资源不可用时，用户和 OpenAPI 必须得到明确原因，例如：

- 库存不足。
- 上游未启用。
- 缺价格。
- 缺资源映射。
- Provider 失败。
- 当前客户不可见。

## 10. 静态住宅代理购买

用户购买静态代理时必须支持：

- 目标业务用途：Telegram、WhatsApp、Amazon、Google、TikTok、其他。
- IP 类型：原生 IP、广播 IP。
- 地区：大洲、国家、州/省、城市。
- 线路：推荐线路、指定线路、Provider 线路。
- 协议：HTTP、SOCKS5、HTTP/SOCKS5 共用。
- 网段：随机或指定网段。
- 时长：30/60/90 天和可配置套餐。
- 数量：受本层可见库存限制。
- 优惠券。
- 支付方式：本层余额或本层支付方式。

订单摘要必须展示：

- 地区。
- IP 类型。
- 线路。
- 协议。
- 网段。
- 时长。
- 数量。
- 优惠券。
- 本层总价。
- 支付方式。
- 库存和上游状态提示。

## 11. 我的静态代理

列表字段：

- IP 地址、端口、账号、密码。
- 地区、业务用途、剩余天数、状态。
- 用户备注、代理 ID、订单号。
- 本层上游、上游订单号、上游实例 ID。
- 创建时间、过期时间、操作。

状态：

- 交付中。
- 正常使用。
- 即将过期。
- 已过期。
- 交付失败。
- 释放中。
- 已释放。

批量能力：

- 批量复制配置。
- 批量下载 TXT/Excel。
- 批量续费。
- 批量修改密码。
- 批量修改备注。
- 批量更换 IP。

导出格式至少支持：

- `IP:PORT`
- `IP:PORT:USERNAME:PASSWORD`
- `USERNAME:PASSWORD@IP:PORT`
- HTTP URL
- SOCKS5 URL

复制和生命周期体验必须对齐 IPIPD 风格：

- IP 开出来后，用户可以直接一键复制常用格式。
- 切 IP 成功后弹出新 IP 信息，并提供同样的复制格式。
- 续费、改密、换 IP 完成后，页面必须展示成功项、失败项、失败原因和下一步操作。
- 列表必须支持按 IP、订单号、国家、状态、到期时间搜索和筛选。

## 12. OpenAPI

每层平台都必须提供本层 OpenAPI。第一阶段公开 API 要按 985Proxy 风格设计，不暴露“第几级代理”“父级代理树”这类概念。

第一阶段至少覆盖：

- 静态业务列表：`POST /res_static/business`。
- 静态库存查询：`POST /res_static/inventory`。
- 静态价格计算：`POST /res_static/calculate`。
- 静态购买下单：`POST /res_static/buy`。
- 静态续费下单：`POST /res_static/renew`。
- 订单结果查询：`POST /res_static/order_result`。
- 订单列表查询：`POST /res_static/order_list`。
- 已购 IP 列表：`POST /res_static/ip_list`。
- IP 详情：`POST /res_static/ip_detail`。
- 更改代理账密：`POST /res_static/change_auth`。
- 可更换 IP 列表：`POST /res_static/switch_ip_list`。
- 更换 IP：`POST /res_static/switch_ip`。
- 导出代理连接信息。
- 查询钱包余额。
- 查询账务记录。
- 为分销商分站提供稳定上游能力，使 Reseller 等分站可以直接把本层 API 当成上游。

APIKey 能力：

- 第一阶段兼容 985Proxy：使用 `apikey` 请求头。
- 内部可以继续支持 App ID / Secret 和签名能力，但不要求下游为了普通下单先接复杂签名。
- APIKey 只展示一次。
- APIKey 哈希存储。
- 权限范围。
- IP 白名单。
- 回调地址。
- 启用 / 禁用。
- 轮换。
- 调用日志。
- 限流事件。

OpenAPI 必须有：

- Swagger / OpenAPI 文档。
- 985Proxy-compatible 字段映射说明。
- 错误码说明。
- `apikey` 鉴权说明。
- SDK 示例。
- AI 可读文档。

## 13. Admin 运营后台

Admin 必须支持完整运营闭环。

### 13.1 上游和 Provider

- 上游 API 配置。
- Provider 配置。
- 凭据加密保存和轮换。
- 连接测试。
- Dry-run 下单。
- 余额查询。
- 能力矩阵。
- 健康检查。
- 请求日志。
- 不可售原因。

### 13.2 资源和库存

- 上游资源库。
- 平台资源树。
- Provider / 上游资源映射。
- 库存快照。
- 同步任务。
- 权重、优先级、上下架。
- 客户可见性控制。

### 13.3 价格中心

- 全局价格模板。
- 资源价格条目。
- 分等级价格表。
- 全局价格覆盖。
- 用户价格模板绑定。
- 用户资源覆盖价。
- 按国家 / 地域 / 可用区 / IP 类型 / 时长 / 数量维护价格。
- 优惠券。
- 折扣窗口。
- 报价沙盒。
- 价格变更审计。

### 13.4 订单和履约

- 订单列表 / 详情。
- 订单项。
- 本层支付单 / 钱包账本。
- 上游订单镜像。
- 上游请求日志。
- 静态代理实例镜像。
- 队列任务。
- 重试、退款、复核、手动补单。
- 部分交付和 item 级失败原因。
- 代客下单：Admin 或代理商从客户详情进入购买流程，不能手输数据库 ID。
- IP 续费搜索：按 IP、订单号、客户、国家、到期时间快速定位可续费资源。
- 切 IP 后弹窗展示新代理并提供复制。

### 13.5 用户、钱包和 APIKey

- 用户列表和详情。
- 代理商租户列表和详情。
- 客户归属代理商租户。
- 用户状态、KYC、风险状态。
- 钱包余额和账本。
- 人工充值、扣款、调账。
- APIKey 签发、禁用、轮换。
- impersonation。
- 审计日志。

### 13.7 分站运营台

Reseller 分站运营台必须接近主站运营台，差异只在数据范围：

- 分站平台级 Admin 看全站点、全部代理商租户、公共上游、公共资源和全站统计。
- 代理商租户 Admin 只看自己的客户、价格、订单、充值、APIKey、代理实例和统计。
- 所有列表、详情、导出、写操作都必须带租户边界。
- 价格配置必须支持全局价格、价格模板、用户价格覆盖，并能按国家、地域、可用区和 IP 类型设置。
- 用户管理、订单管理、统计、充值、退款、补单、续费、切 IP、改密、复制导出都不能因为是分站而缺失。

### 13.6 运维和风控

- KYC 审核。
- 工单回复和关闭。
- 推广和返佣明细。
- 支付异常和对账。
- API 调用日志。
- 速率限制事件。
- 系统健康状态。
- 全站公告和维护模式。

## 14. 安全与审计

必须实现：

- 上游 APIKey 加密存储。
- Provider 凭据加密存储。
- 支付密钥加密存储。
- APIKey 哈希存储，只展示一次。
- Provider / 上游请求响应脱敏日志。
- 支付通知原文留档。
- 用户导出代理时做权限校验。
- 管理员高危操作审计。
- 钱包调整必须 reason + idempotencyKey。
- 退款、补单、credential rotate、impersonation 必须可追踪。

## 15. 验收边界

V1 不能只展示页面，必须形成真实闭环。

静态住宅主线验收：

- 用户能注册登录、充值、购买、查看、复制、导出静态代理。
- PR、IPIPD、985Proxy 只展示各自真实覆盖的首批国家，IPIPD 只展示推荐 IP 能力。
- Admin 能配置上游、同步资源、编排资源树、设置价格、处理订单。
- 用户级价格覆盖、全局价格覆盖、价格模板和分等级价格表能影响真实报价。
- 报价来自本层价格系统，不使用静态 fallback。
- 下单扣本层余额，不重复扣款。
- 向直接上游采购，不越级访问。
- 上游交付后保存本层订单镜像和代理实例镜像。
- IP 开出后可直接复制；切 IP 后弹窗显示新代理并可复制。
- 代客下单和 IP 续费搜索不需要手输数据库 ID。
- 上游失败进入可处理状态，不生成假代理。

985Proxy-compatible OpenAPI 验收：

- 下游分站可以通过上游 APIKey 调用 `/res_static/*` 获取业务、库存、报价和下单。
- API 兼容 985Proxy 风格的 `apikey` header 和 `code/msg/data` 响应。
- 上游只识别直接 APIKey 客户。
- 下游不知道上游的上游 APIKey。
- 每层都有自己的订单、账本和镜像。
- Reseller 这类共享分站可以承载多个代理商租户。
- 同一共享分站内，不同代理商的客户、余额、价格、APIKey、订单、代理镜像和审计互相隔离。
- 不需要全局多级代理树。

Admin 验收：

- 不需要手输数据库 ID 完成核心运营动作。
- loading、empty、error、success 状态清楚。
- 危险操作有确认、原因、结果反馈和审计。
- 无静态 fallback 业务数据。
- 权限错误、上游错误、库存不足和价格缺失都有可执行处理方式。

动态住宅验收：

- 未生产化时只能展示真实暂缓状态。
- 不允许假流量、假账号、假提取链接或假代理。
