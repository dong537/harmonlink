# IPIPX / IPEasy 前端页面设计提示词

本文件基于 `PRD.md`、`docs/BLUEPRINT.md`、`REWRITE_PROJECT_PROMPT.md`、`EXECUTION_PLAN.md` 和 `.trellis/spec/frontend-ui-ux.md`，用于交给 UI 设计模型、Figma 生成器或前端 Agent 进行页面设计；只做设计和提示词，不写代码、不生成假数据。

## 前端总提示词

```text
你现在是 IPIPX / IPEasy 家宽代理平台的资深 UI/UX 设计师与 SaaS 产品设计顾问。请基于以下产品背景，为新项目设计 Public 官网、Customer 控制台、Admin 运营后台三类前端 surface 的页面方案。

项目定位：IPIPX 是一个 985Proxy-compatible OpenAPI 家宽代理平台，面向静态住宅代理交易、交付和分销。每一层站点既是 Upstream Consumer，又是 Downstream Provider。平台必须支持主站和 Reseller 分站，多租户边界、钱包账本、APIKey、订单、履约、审计等能力需要在设计中体现。

重要约束：
1. 只做 UI/UX 页面设计、信息架构、交互状态和设计说明，不写业务代码。
2. 不生成假数据，不用静态假库存、假余额、假订单、假代理、假国家数量、假实时覆盖率冒充真实能力。
3. 所有国家、库存、价格、延迟、可用率、余额、订单、APIKey、审计记录等业务数据都必须标注为来自真实 Provider 库存、运营配置、OpenAPI、钱包、账本或审计日志。
4. 未接入或第一阶段不实现的能力必须设计成明确的暂缓、未启用、无权限、配置缺失或不可用状态，不能用漂亮占位数据掩盖。
5. 前端权限只影响可见性和导航裁剪，真实权限由后端 RBAC / Scope guard 决定。
6. 所有列表必须设计 loading、empty、error、permission、pagination、filter/search 状态。
7. 所有表单必须设计 label、校验、提交中、错误、成功反馈。
8. 所有用户可见文案应预留 i18n，不要把文案设计成散落硬编码。
9. 设计必须区分 server state、form state、client state：server state 来自 API / TanStack Query，form state 用表单校验，client state 只保存 drawer、tab、selected rows 等 UI 状态。
10. Admin 是高密度运营台，不套官网 hero 风格；Customer 是工作台式购买和钱包流程；Public 可以参考 IPEasy 气质。

品牌与视觉方向：
- 品牌名默认使用 IPEasy，可配置为 IPIPX 或其他分站品牌。
- Public / Customer 可以参考 IPEasy 的干净商业 SaaS 气质。
- 主色：#0040ff / #003afe。
- 文字主色：#101010。
- 正文灰：#747689。
- 浅背景：#f7f9fc。
- 字体优先 Inter + Urbanist，中文 fallback 使用系统无衬线：-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif。
- 视觉风格：白底、大留白、浅灰分区、细边框卡片、轻阴影、8-12px 圆角。
- 避免渐变大背景、装饰 orb、过度玻璃拟态、过度营销化视觉。

请输出：
1. 三个 surface 的整体信息架构。
2. Public / Customer / Admin 的页面清单和导航结构。
3. 每个关键页面的布局结构、核心模块、状态设计、交互行为。
4. loading、empty、error、permission、success、pending、disabled、not configured 等状态如何出现。
5. 哪些数据来自真实 API / Provider / 钱包 / 账本 / 审计 / 运营配置。
6. 哪些能力属于第一阶段 MVP，哪些能力仅作为第二阶段或未来能力入口。
7. 组件层级建议：Route Shell、Feature Container、Presentational Component。
8. 可访问性与 i18n 注意事项。

不要输出代码，不要输出 mock 数据，不要输出实现计划，不要创建数据库字段，不要生成 API 示例假响应。
```

## 第一阶段 MVP 页面设计提示词

```text
请为 IPIPX / IPEasy 家宽代理平台设计第一阶段 MVP 页面。第一阶段目标不是完整代理购买和 Provider 履约，而是交付真实工程骨架、认证权限、APIKey、钱包、支付单、账本、审计等基础闭环的最小可用页面。

第一阶段明确包含：
- 登录 / 注册 / 退出等基础认证入口。
- Customer 最小页面：登录、余额、充值单、钱包流水。
- Admin 最小页面：登录、用户列表、钱包流水、支付单、审计日志。
- 权限错误、服务错误、空数据、加载中、提交中、成功反馈。
- 单币种钱包和账本展示。
- APIKey / 权限入口可以设计为状态或导航项，但不需要做完整下单履约页面。

第一阶段明确不包含：
- 静态住宅代理真实购买。
- Provider 真实履约。
- 假代理实例、假订单、假库存、假余额。
- 动态住宅代理生产化。
- 完整资源映射工作台。
- 支付宝 / 易支付真实线上收款闭环。

请按以下范围设计页面：

1. Public 最小入口
- 首页的基础品牌展示、登录 / 控制台入口、API 文档入口、能力说明。
- 对静态住宅代理能力可以展示产品方向，但涉及国家、库存、价格、覆盖率时必须标注“来自真实 Provider / 运营配置”，没有接入时展示“暂未接入 / 配置后展示”。
- 动态住宅能力只显示暂缓或灰度状态，不展示假账号、假流量、假 endpoint。

2. Customer MVP
- 登录页。
- 控制台概览：余额卡片、最近充值单、最近钱包流水、APIKey 状态入口、动态能力暂缓提示。
- 钱包页：可用余额、冻结余额、币种、更新时间；必须从 wallet 读取。
- 充值单页：创建充值单入口、充值单列表、状态筛选、提交中、创建成功、创建失败。
- 流水页：ledger entries 列表，展示充值、调整、退款等类型；支持分页、筛选、错误态。
- 所有数据为空时要区分“暂无记录”和“服务错误 / 权限不足”。

3. Admin MVP
- Admin 登录页。
- Admin Shell：高密度 sidebar/topbar，显示当前管理员身份、站点 / 租户上下文。
- 用户列表：筛选、搜索、分页、状态、所属站点 / 租户、钱包摘要入口；不允许手输数据库 ID 作为主要操作路径。
- 用户详情抽屉：基础信息、钱包摘要、最近流水、最近支付单、审计入口。
- 钱包流水：按用户、租户、类型、时间筛选；失败不能显示假空列表。
- 支付单：状态筛选、创建时间、金额、币种、确认 / 拒绝操作入口；高危操作必须有确认、reason、pending、结果反馈、audit log 提示。
- 审计日志：actor、tenant/site、target user、business object、action、reason、requestId、createdAt。

设计要求：
- Admin 使用高密度运营台模板：sidebar/topbar + toolbar + filters + table + drawer/modal + audit timeline。
- Customer 使用清晰工作台模板：卡片摘要 + 列表 + 表单 + 状态反馈。
- 所有金额使用 decimal string 视觉呈现，不暗示前端用 JS number 计算货币。
- 时间按 locale 展示，但标注源数据为 ISO 8601 UTC。
- 每个页面都要说明 loading、empty、error、permission、success 状态。
- 不要写代码，不要生成假数据，不要输出实现计划。
```

## Public 官网页面提示词

```text
请为 IPIPX / IPEasy 设计 Public 官网页面。Public 端用于品牌展示、搜索转化、购买入口、工具、帮助和 API 文档入口，可以参考 IPEasy 的干净 SaaS 气质，但不能照抄旧项目代码或生成假业务数据。

视觉风格：
- 白底、大留白、浅灰分区、细边框卡片、轻阴影、8-12px 圆角。
- 主色 #0040ff / #003afe，文字主色 #101010，正文灰 #747689，浅背景 #f7f9fc。
- 字体优先 Inter + Urbanist，中文 fallback 使用系统无衬线。
- 首页首屏不是营销花活，而是强产品信号：Logo、导航、语言 / 登录入口、中心大标题、明确 CTA、4 个能力卡片。
- 避免渐变大背景、装饰 orb、过度玻璃拟态。

Public 推荐导航：
- 首页
- 购买
- 工具
- 帮助
- 推广 / 合作
- API 文档
- 登录 / 控制台

首页结构：
1. Header
- Logo / 品牌名 IPEasy 或 IPIPX。
- 导航、语言切换、登录、控制台 CTA。
- 分站品牌应支持来自站点配置。

2. Hero
- 文案方向：全球原生住宅 IP、动静态全覆盖、城市级库存、API 自动化、7x24 支持。
- CTA：立即进入控制台、查看 API 文档、查看可售资源。
- 注意：如果没有真实 Provider 库存或运营配置，不展示“200+ 国家”“实时库存”等假数字；可改为“覆盖范围由已启用 Provider 配置展示”。

3. 能力卡片
- 静态住宅代理。
- 985Proxy-compatible OpenAPI。
- 钱包 / 账本 / 审计。
- Reseller 分站与租户隔离。
- 卡片只描述能力和状态，不填假库存、假价格。

4. 为什么选择
- 真实 Source of Truth。
- APIKey hash 与权限范围。
- 账本审计。
- 上游请求脱敏日志。
- Provider 未启用时 fail closed。

5. 全球代理池 / 国家库存网格
- 设计国家、地区、库存、延迟、可用率、可售状态、库存更新时间、不可售原因的网格。
- 明确标注数据来自真实 Provider 库存或运营配置。
- 未接入时显示“暂未接入 / 未启用 Provider / 缺库存同步 / 缺价格 / 缺资源映射”，不能显示静态假数字。

6. 产品用途 / 工具 / 教程
- 浏览器代理、指纹浏览器、API 自动化、代理检测工具、静态代理教程。
- 工具页面可以设计输入框、校验、结果区域、错误态，但不要生成假检测结果。

7. Pricing / 购买入口
- 静态住宅按地区、时长、数量展示计费入口。
- 价格来自本层价格系统或运营配置。
- 动态住宅必须标注暂缓或灰度状态，未接入时不展示假流量包或假 endpoint。

8. API 文档入口
- 985Proxy-compatible 风格：apikey header、code/msg/data envelope、/res_static/* 路由。
- 页面可以展示文档入口和鉴权说明，但不要编造真实 APIKey 或假响应数据。

9. CTA 与 Footer
- 蓝色 CTA 区块，登录 / 注册 / 联系支持。
- Footer 包含产品、工具、帮助、条款、支持、品牌配置。

状态设计：
- 页面级 loading。
- 配置缺失状态。
- Provider 未启用状态。
- 库存同步失败状态。
- 权限或登录引导状态。
- 空配置状态。

请输出每个 Public 页面或区块的布局、模块、文案方向、数据来源说明和状态设计。不要输出代码，不要生成假数据，不要输出实现计划。
```

## Customer 控制台页面提示词

```text
请为 IPIPX / IPEasy 设计 Customer 控制台页面。Customer 控制台是用户购买、钱包、订单、代理实例、APIKey、工单和账户的工作台。设计应清晰、可反馈、可恢复，不做纯营销页面。

Customer 推荐导航：
- 概览
- 购买静态住宅 IP
- 订单
- 代理
- 钱包
- 工单
- 教程
- 账户

第一阶段只实现登录、余额、充值单、流水；购买静态住宅 IP、订单、代理实例、复制导出等能力可设计为未来页面或禁用入口，但不能展示假订单、假代理、假库存。

整体布局：
- 左侧或顶部导航，移动端适配用户核心流程。
- 顶部显示品牌、当前用户、余额摘要、通知、语言切换、退出。
- 内容区以卡片、表单、列表、步骤条和状态提示为主。
- Customer 页面要比 Admin 更舒展，但仍保持工作台效率。

关键页面：

1. 登录 / 注册
- 登录表单：邮箱 / 用户名、密码、提交中、错误、成功跳转。
- 注册表单：基础字段、条款确认、校验、错误态。
- 忘记密码 / 安全提示。
- 不展示 fake session 或 fake user。

2. 概览
- 余额卡片：available、frozen、currency、updatedAt，来源为 wallet。
- 近期充值单，来源为 payment_orders。
- 近期钱包流水，来源为 ledger_entries。
- APIKey 状态入口：已签发 / 未签发 / 禁用 / 需要权限。
- 静态代理能力入口：第一阶段可显示“购买功能第二阶段接入 / Provider 未启用 / 配置后可用”。
- 动态住宅能力状态：暂缓接入或 UNSUPPORTED_CAPABILITY，不显示假账号、假流量、假 endpoint。

3. 购买静态住宅 IP（第二阶段设计入口）
- 目标业务用途、IP 类型、地区、线路、协议、网段、时长、数量、优惠券、支付方式。
- 报价来自 quote use case，不能由前端本地计算最终价格。
- 库存来自 inventory_snapshots 或 Provider / 运营配置。
- 订单摘要展示地区、IP 类型、线路、协议、网段、时长、数量、优惠券、本层总价、支付方式、库存和上游状态。
- 必须设计 loading、quote、余额不足、库存不足、缺资源映射、缺价格、Provider 未启用、支付中、交付中、失败退款、成功反馈。
- 第一阶段如果未接入，应呈现禁用状态和真实原因，而不是假购买流程。

4. 订单
- 订单列表：订单号、状态、金额、币种、资源、数量、创建时间、支付状态、履约状态。
- 订单详情：报价镜像、支付信息、订单项、履约状态、失败原因、审计提示。
- 上游失败进入可处理状态，不生成假代理。

5. 我的静态代理
- 列表字段：IP 地址、端口、账号、密码、地区、业务用途、剩余天数、状态、用户备注、代理 ID、订单号、创建时间、过期时间、操作。
- 状态：交付中、正常使用、即将过期、已过期、交付失败、释放中、已释放。
- 操作：复制、导出、续费、改密、切 IP、备注。
- 复制格式：IP:PORT、IP:PORT:USERNAME:PASSWORD、USERNAME:PASSWORD@IP:PORT、HTTP URL、SOCKS5 URL。
- 切 IP 成功后弹窗展示新 IP、端口、账号、密码、协议、到期时间，并提供复制格式。
- 第一阶段不可展示假代理；未接入时显示真实暂缓状态。

6. 钱包
- 余额、冻结余额、币种、最近更新时间。
- 创建充值单入口。
- 充值单列表：金额、币种、状态、创建时间、确认时间、渠道或人工确认状态。
- 钱包流水：类型、金额、余额变化、业务对象、reason、requestId、createdAt。
- 创建充值单不改变钱包余额；支付确认后才更新 wallet + ledger。

7. APIKey 管理
- APIKey 明文只展示一次。
- 展示 scope、ip whitelist、状态、createdAt、rotatedAt。
- 设计签发、禁用、轮换、复制一次性明文、调用日志入口。
- 不生成假 APIKey。

8. 工单 / 教程 / 账户
- 工单列表、创建工单、状态、回复。
- 教程：浏览器、指纹浏览器、API、静态代理使用说明。
- 账户：资料、安全、语言、通知。

状态要求：
- 所有列表有 loading、empty、error、permission、pagination、filter/search。
- 所有表单有 label、校验、提交中、错误、成功反馈。
- 权限不足不能显示为空数据。
- 服务错误不能显示为“暂无记录”。
- 文案预留 i18n。
- 支持 keyboard、focus、aria、reduced motion。

请输出 Customer 控制台的页面结构、布局、关键组件、状态设计、数据来源说明和第一阶段 / 第二阶段边界。不要输出代码，不要生成假数据，不要输出实现计划。
```

## Admin 运营后台页面提示词

```text
请为 IPIPX / IPEasy 设计 Admin 运营后台页面。Admin 是高密度运营工作台，不是官网皮肤，也不只是表格堆砌。它必须支持平台管理员、分站平台级 Admin、代理商租户 Admin 在不同数据范围内完成真实运营动作。

Admin 推荐导航：
- 概览
- 用户
- 租户 / 分站
- 供应商
- 资源库存
- 价格
- 订单履约
- 支付钱包
- 工单
- 审计日志
- 系统设置

整体布局：
- 高密度 sidebar/topbar。
- 顶部显示当前 operator、角色、站点 / 租户上下文、环境状态、系统健康、语言、退出。
- 页面模板：toolbar + filters + table + drawer/modal + audit timeline。
- 列表优先可扫描、可筛选、可分页、可定位。
- 高危操作必须有确认、reason、pending 状态、结果反馈、audit log。
- Admin 不应要求管理员手输数据库 ID 完成核心动作；应从用户详情、订单详情、代理详情进入操作。

关键页面：

1. Admin 登录
- 登录表单、错误态、提交中、安全提示。
- 权限不足、登录过期、被禁用状态。

2. 概览
- 平台健康、Provider 状态、库存同步状态、支付单待处理、钱包异常、订单履约异常、审计告警。
- 第一阶段可以只显示认证、钱包、支付单、审计等已接入状态。
- 未启用 Provider / 未接入履约必须显示真实未启用原因，不展示假成功率。

3. 用户管理
- 用户列表：搜索、状态、站点、租户、余额摘要、APIKey 状态、风险状态、创建时间。
- 用户详情抽屉：基础信息、租户归属、钱包、流水、支付单、订单、APIKey、审计。
- 代客下单入口：第二阶段能力，从用户详情进入，不手输数据库 ID。
- 用户级价格覆盖入口：第二阶段或后续能力。
- impersonation 必须有权限校验、reason、审计提示。

4. 租户 / 分站
- 分站站点配置、品牌、域名、公告、维护状态。
- 代理商租户列表：状态、客户数量、余额汇总、订单汇总、权限范围。
- 分站平台级 Admin 可看全站；代理商租户 Admin 只能看自己的客户、价格、订单、充值、APIKey、代理实例和统计。
- 所有列表和操作必须体现 tenant/site 边界。

5. 供应商 / 上游配置
- Provider Registry：Provider code、状态 ACTIVE / DISABLED、baseUrl、能力矩阵、健康检查、库存同步状态、最近错误。
- 凭据只显示已配置 / 未配置 / 需轮换，不显示 secret 明文。
- 连接测试、dry-run 下单、余额查询、请求日志入口。
- Provider 未启用时购买路径 fail closed。
- 上游请求 / 响应日志必须脱敏。

6. 资源库存
- 上游资源库与本层平台资源树。
- 国家、城市、线路、可用区、IP 类型、库存、capturedAt、freshness、可售状态、不可售原因。
- ResourceMapping 工作台：左侧本层资源树，右侧上游资源库，支持映射、权重、上下架、客户可见性。
- 缺资源映射、缺库存、Provider 未启用、库存过期、缺价格都要有明确状态。

7. 价格中心
- 全局价格模板、资源价格条目、分等级价格表、全局覆盖价、用户价格模板绑定、用户资源覆盖价。
- 按国家 / 地域 / 可用区 / IP 类型 / 时长 / 数量维护价格。
- 报价沙盒：选择用户、资源、时长、数量后查看最终价格来源和优先级。
- 价格变更必须设计 audit trail。
- 金额用 decimal string 展示，不暗示前端用 JS number 算钱。

8. 订单履约
- 订单列表、订单详情、订单项、本层支付单、钱包账本、上游订单镜像、上游请求日志、静态代理实例镜像、队列任务。
- 支持重试、退款、复核、手动补单，但所有高危操作必须 reason + audit。
- 部分交付和 item 级失败原因必须可见。
- IP 续费搜索：按 IP、订单号、客户、国家、到期时间快速定位可续费资源。
- 切 IP 后弹窗展示新代理并提供复制格式。
- 第一阶段未接入履约时显示暂缓状态，不生成假订单和假代理。

9. 支付钱包
- 支付单列表：状态、金额、币种、用户、租户、创建时间、确认时间、requestId。
- 钱包列表和用户钱包详情：available、frozen、currency、updatedAt。
- 流水列表：充值、扣款、退款、调整、冻结、解冻。
- 人工充值、扣款、调账必须有 reason、idempotencyKey、确认、pending、结果反馈、审计日志。
- 创建充值单不改钱包；确认成功后才写 wallet + ledger。

10. 工单
- 工单列表、状态、优先级、用户、租户、最后回复时间。
- 回复、关闭、转交、审计。

11. 审计日志
- actor、tenant/site、target user、business object、action、reason、requestId、createdAt。
- 支持按 actor、租户、动作、业务对象、时间筛选。
- 高危操作详情页展示关联用户、钱包、订单、Provider 请求。

12. 系统设置
- 平台币种、站点品牌、公开配置、维护模式、API 文档入口、health/ready 状态。
- Web 只允许公开配置；API / Worker 才能读取 secret。
- secret 不进入日志、OpenAPI、测试快照、前端 bundle。

状态要求：
- 所有列表必须有 loading、empty、error、permission、pagination、filter/search。
- 服务错误不能变成空表。
- 权限错误不能变成不可见假空数据。
- 缺配置、未启用、未同步、库存过期、价格缺失、资源映射缺失都要有可执行处理方式。
- 所有写操作都有提交中、成功、失败、审计反馈。
- 文案预留 i18n；控件支持 keyboard、focus、aria、reduced motion。

请输出 Admin 运营后台的信息架构、页面布局、表格字段、筛选项、抽屉 / 弹窗交互、状态设计、权限边界和审计提示。不要输出代码，不要生成假数据，不要输出实现计划。
```

## 推荐使用顺序

1. 先使用“前端总提示词”统一产品背景、视觉方向、数据真实性和三类 surface 边界。
2. 再使用“第一阶段 MVP 页面设计提示词”收敛第一阶段可交付页面，避免把代理购买和 Provider 履约提前做成假 UI。
3. 使用“Public 官网页面提示词”设计官网、购买入口、工具、帮助和 API 文档入口。
4. 使用“Customer 控制台页面提示词”设计用户工作台、钱包、充值、订单、代理和 APIKey 体验。
5. 使用“Admin 运营后台页面提示词”设计高密度运营台、权限边界、资金操作、资源库存、价格、履约和审计页面。
6. 每次交给 UI 设计模型、Figma 生成器或前端 Agent 前，都要再次强调：只做设计，不写代码；不生成假数据；未接入能力必须显示真实状态。
